{% extends 'base.html.twig' %}

{% block title %}Modifier utilisateur{% endblock %}

{% block body %}
    <h1>Modifier utilisateur n°{{client.id}}</h1>

    {{ include('admin/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin-index') }}">back to list</a>

    {{ include('admin/_delete.html.twig') }}
{% endblock %}